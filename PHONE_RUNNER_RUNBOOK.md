# Phone Runner 操作手冊

> 本文件是給使用者與後續 AI 的固定操作規則。正式流程不使用 Automate、ntfy 或 Termux:Tasker。
> 
> 最後更新：2026-09-02（台灣時間）

## 一、平常使用與本對話排程

- 排程時間由使用者在本對話決定，例如「設定 1 分鐘」、「設定 10 分鐘」或「每天 21:30」。
- 後續 AI 負責把設定寫入正式控制流程、送到手機並回報結果；使用者不需要複製 FCM token、不需要貼 GitHub 內容、不需要重新配對。
- 正式遠端流程為：GitHub `remote_request.json` → GitHub Webhook → Cloudflare Worker → FCM → APK → Termux → `phone_tick.sh` → GitHub 回報。
- 原生排程由 APK 直接設定 Android AlarmManager。Termux 不再負責透過 `am broadcast` 設定排程；這是因為 Xiaomi/MIUI 會拒絕 Termux UID 的跨使用者廣播。
- 使用者只需要在 APK 看到結果；除非本手冊列出按鈕，否則不要自行操作。

## 二、手機「重新開機」後

### 要做

1. 等待手機完成開機，並等待約 30–60 秒讓 Phone Runner、Termux 與網路恢復。
2. 不必立刻開啟 App。若要檢查，開啟 **Android Phone Runner**。
3. 先看狀態燈：
   - 全部為綠色：不需要按任何設定按鈕；可按 **重新整理** 作唯讀確認。
   - 有紅色：只按對應的修復按鈕，完成後按 **測試 TERMUX 橋接**，最後按 **重新整理**。
4. 確認小米的 Phone Runner 與 Termux 仍允許自啟動、背景執行、電池不受限制；只有狀態變紅或系統收回權限時才重新設定。
5. 等待遠端喚醒或本對話指定的排程結果。APK 的開機接收器會自動恢復原生 AlarmManager 排程，不需手動重設。

### 不要做

- 不要重新安裝 APK。
- 不要清除 App 資料或解除安裝。
- 不要重新配對、輸入配對碼或覆蓋裝置註冊。
- 不要複製 FCM token。
- 不要按 **立即執行 RUNNER**，除非本對話明確要求手動安全測試。
- 不要因為重開機就按 **儲存並設定原生排程**；只有排程真的遺失，或本對話要求改時間時才按。

## 三、正式 APK「更新」後（保留原 App 的一般更新）

這是正常的簽名更新，例如 v0.2.6 → v0.2.7。原則是保留資料與註冊，不重新配對。

### 要做

1. 在 App 按 **檢查 APK 更新**。
2. 若出現下載確認，按下載／繼續。
3. Android 安裝畫面出現後，只按一次 **安裝** 或 **更新**。
4. 安裝完成後按 **開啟**，或回到桌面開啟 **Android Phone Runner**。
5. 依序查看狀態：
   - **授權通知與 TERMUX**：只有通知或 Termux 狀態為紅色才按，並接受系統提示。
   - **允許 PHONE RUNNER 背景執行**：只有背景／電池狀態為紅色才按。
   - **開啟小米自啟動／背景設定**：只有 Xiaomi 自啟動或背景狀態為紅色才按。
   - **開啟 TERMUX 系統設定**：只有 Termux 狀態為紅色才按；確認 `allow-external-apps=true)，並把 Termux 電池設為不受限制。
6. 按 **測試 TERMUX 橋接**。
7. 按 **重新整理**，確認裝置仍顯示 `REGISTERED) 且狀態燈為綠色。

### 不要做

- 不要先解除安裝舊版。
- 不要清除資料。
- 不要重新配對或填入一次性配對碼。
- 不要手動貼 Worker URL。
- 不要複製或重新填 FCM token。
- 不要為了更新重新按所有按鈕；只處理紅色項目。
- 不要在安裝完成後立刻按 **立即執行 RUNNER**，除非本對話要求安全驗證。

### 若 Android 顯示「不允許安裝未知應用程式」

只需依 Android 提示，允許「Phone Runner」安裝未知應用程式一次，返回安裝畫面再按 **安裝／更新**。這不是重新配對，也不需要清除資料。

## 四、真的「解除安裝後重新安裝」

只有在 Android 明確無法以簽名更新覆蓋，或使用者主動解除安裝時才走這條路。這可能清掉本機權限與註冊資料。

### 要做

1. 安裝完成後開啟 **Android Phone Runner**。
2. 按 **授權通知與 TERMUX**，接受系統權限。
3. 按 **允許 PHONE RUNNER 背景執行**，設定電池不受限制。
4. 按 **開啟小米自啟動／背景設定**，允許自啟動與背景活動。
5. 若 Termux 狀態為紅色，按 **開啟 TERMUX 系統設定**，確認 `allow-external-apps=true) 與電池不受限制。
6. 按 **測試 TERMUX 橋接**。
7. 按 **重新整理**，等待顯示 `裝置已登記: REGISTERED)。
8. 若狀態仍顯示未登記，先不要反覆按註冊或複製 token；由本對話檢查 Worker registry，再決定是否需要清理失效註冊。
9. 只有本對話要求時，才在分鐘欄位輸入數字並按 **儲存並設定原生排程**。

### 不要做

- 不要自行輸入配對碼。
- 不要按 **複製 FCM TOKEN**。
- 不要同時安裝多個 Phone Runner 版本。
- 不要在尚未完成橋接測試前反覆送遠端任務。
- 不要因為畫面暫時顯示紅燈就清除資料；先按對應設定按鈕，再按橋接測試與重新整理。

## 五、按鈕快速判斷

| 畫面狀況 | 要按 | 不要按 |
|---|---|---|
| 全部狀態綠色 | **重新整理**（可選） | 其他設定按鈕、複製 FCM TOKEN |
| 通知或 Termux 紅色 | **授權通知與 TERMUX** | 重新配對 |
| 背景／電池紅色 | **允許 PHONE RUNNER 背景執行** | 解除安裝 |
| Xiaomi 自啟動紅色 | **開啟小米自啟動／背景設定** | 清除資料 |
| Termux 紅色 | **開啟 TERMUX 系統設定**，再 **測試 TERMUX 橋接** | 直接重送業務任務 |
| 要確認橋接 | **測試 TERMUX 橋接** | **立即執行 RUNNER** |
| 本對話指定新週期 | 輸入分鐘後按 **儲存並設定原生排程** | 重新配對 |
| 要升級 APK | **檢查 APK 更新**，安裝畫面按一次 **安裝／更新** | 先解除安裝 |

## 六、測試與完成判定

- 安全測試只使用 `runner_check`、排程狀態與橋接檢查，不執行 Facebook 或 Telegram 業務任務。
- 排程測試要看到 GitHub Issue 回報 `SCHEDULE_SET` 或等價的 PASS，並觀察至少三輪 `runner_check`。
- 螢幕關閉測試期間，使用者只需依本對話指示關閉螢幕；不要自行重新安裝或重配對。
- 沒有取得手機實際螢幕／Doze 狀態遙測前，只能宣稱「背景喚醒多輪 PASS」，不能宣稱完整 Doze PASS。
- 正式業務驗收必須在安全排程驗收完成後才進行。
